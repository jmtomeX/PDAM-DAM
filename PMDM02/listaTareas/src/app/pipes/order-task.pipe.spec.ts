import { OrderTaskPipe } from './order-task.pipe';

describe('OrderTaskPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderTaskPipe();
    expect(pipe).toBeTruthy();
  });
});
